# Python
__pycache__/
*.pyc
.venv/
.env

# Jupyter
.ipynb_checkpoints/

# OS/editor cruft
.DS_Store
Thumbs.db

# Data (keep out of git)
data/

# Outputs & artifacts
**/outputs*/
**/pred/
**/samples/

# Checkpoints (track only best_model via LFS)
**/checkpoints/*.pt
!**/checkpoints/best_model.pt

# Kaggle token (never commit)
kaggle.json

# If you kept an old name around, ensure submission_example.csv stays tracked
# (The original submission.csv is still ignored if it reappears by accident)
**/submission.csv
